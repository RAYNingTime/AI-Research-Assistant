# Core AI / API
openai>=1.30.0          # OpenAI SDK — xAI provides an OpenAI-compatible API at api.x.ai/v1
python-dotenv>=1.0.0    # Loads variables from the .env file

# Web scraping (cutting-edge extraction)
trafilatura>=1.12.0     # State-of-the-art boilerplate-removal & text extraction
httpx>=0.27.0           # Modern async-capable HTTP client
lxml>=5.2.0             # Fast XML/HTML parser (trafilatura back-end)
lxml_html_clean>=0.2.0  # HTML cleaning extension for lxml

# Scheduling
APScheduler>=3.10.4     # In-process job scheduler (cron-style daily runs)

# Data handling
pandas>=2.2.0           # CSV reading / data manipulation
